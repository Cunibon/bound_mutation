name: bound_mutation
description: "A small wrapper class around Riverpod Mutations to bind a function to a mutation."
version: 1.0.0
repository: https://github.com/Cunibon/bound_mutation

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
